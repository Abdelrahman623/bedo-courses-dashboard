VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Notes code blocks run on a Judge0-compatible API. Optional: the default
# is Judge0's free public instance. Set this to your own Judge0 if you hit
# rate limits or don't want note code leaving your machine.
# VITE_CODE_RUNNER_URL=https://ce.judge0.com
# Only for a self-hosted Judge0 started with AUTHN_TOKEN (shipped to the browser).
# VITE_CODE_RUNNER_TOKEN=
